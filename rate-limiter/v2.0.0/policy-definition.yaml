name: rate-limiter
version: 2.0.0
description: Rate limiter with context support (breaking changes)
configuration:
  limit:
    type: integer
    description: Maximum requests per window
    required: true
  window:
    type: string
    description: Time window
    required: true
  burst:
    type: integer
    description: Burst capacity
    required: true