name: rate-limiter
version: 1.0.0
description: A simple rate limiter policy
configuration:
  limit:
    type: integer
    description: Maximum number of requests allowed per window
    required: true
  window:
    type: string
    description: Time window for rate limiting (e.g., "1m", "1h")
    required: true